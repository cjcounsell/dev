# WSL Profile Layer

WSL-specific Linux configurations that differ from native Linux systems.

This layer is automatically included for the `wsl` profile via PROFILE_DOTFILES.

## Use Cases

- WSL-specific shell tweaks
- Linux tool configs that need Windows filesystem awareness
- Symlink handling differences
- Path adjustments for /mnt/c access
